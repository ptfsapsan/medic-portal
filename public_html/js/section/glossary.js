$(function (){
   // $('.letter').on('click', function (){
   //    var t = $(this),
   //       letter = t.data('letter'),
   //       list_div = $('#term_list');
   //    list_div.html('').append($('<img />', {
   //       src: '/images/ajax-loader.gif'
   //    }));
   //    $.post('/ajax/load-terms',
   //       {
   //          section_id: vars.section_id,
   //          lang: vars.lang,
   //          letter: letter
   //       },
   //       function (data){
   //          var d = eval('(' + data + ')'),
   //             html = '<ul>';
   //          $.each(d, function (i, v){
   //             html += '<li><a href="' + v.link + '">' + v.title + '</a></li>';
   //          });
   //          html += '</ul>';
   //          list_div.html(html);
   //       }
   //    );
   //    t.addClass('active').siblings().removeClass('active');
   // });
   // $('.letter[data-letter=' + vars.letter + ']').click();

});